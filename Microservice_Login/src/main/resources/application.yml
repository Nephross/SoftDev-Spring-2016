## YAML Template.
---
# HTTP Server
server:
  port: 8093   # HTTP (Tomcat) port

# Spring properties
spring:
  application:
     name: microserviceLogin

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
