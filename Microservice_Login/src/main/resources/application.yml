## YAML Template.
---
# HTTP Server
server:
  port: 8091   # HTTP (Tomcat) port

# Spring properties
spring:
  application:
     name: microserviceSkel1

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
